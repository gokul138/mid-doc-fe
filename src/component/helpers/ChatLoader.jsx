import React from 'react';
import './chatLoader.css'; // Assuming the CSS is in a file named ChatLoader.css

const ChatLoader = () => {
  return (
    <div className="chat-loader"></div>
  );
};

export default ChatLoader;
